model Category {
    id String @id @default(uuid())
    name String @unique
    slug String 
    icon String?

    gearItems GearItem[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("categories")
}